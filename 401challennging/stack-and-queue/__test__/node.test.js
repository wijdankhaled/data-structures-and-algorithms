'use strict'

const Node=require('../node');

discibe('Node calss',()=>{
it('',()=>{
    let value ='some value';
    let newNode= new Node (value);
    expect(newNode.value).toEqual(value);
    expect(newNode.next).toBe();

} )
})